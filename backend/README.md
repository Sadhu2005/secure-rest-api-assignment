# Secure REST API Assignment

This repository contains a starter scaffold for a full‑stack application.

## Backend
- Express server (`src/app.js`)
- Prisma ORM (`prisma/schema.prisma`)
- Environment configuration (`.env`)
- Basic folder structure for controllers, routes, middlewares, services, utils, and config.

## Frontend
- React application (`src/App.jsx`)
- Placeholder directories for components, pages, and services.

You can install dependencies and start the servers with:
```bash
cd backend && npm install && npm run dev
cd ../frontend && npm install && npm start
```
